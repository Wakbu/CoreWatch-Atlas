# v1.1.11

## Linux Agent service update handoff / Linux Agent 서비스 업데이트 handoff

- Linux 설치 서비스에 Agent 작업 디렉터리를 명시해 업데이트 handoff가 `/`이 아닌 Agent 설치 경로에서 실행되도록 수정했습니다.
- The Linux installation service now sets the Agent working directory so update handoff executes from the Agent installation path rather than `/`.
