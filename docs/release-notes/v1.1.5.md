# v1.1.5

## Agent 업데이트 패키지 / Agent update package

- Linux와 Windows 모두에서 올바르게 해제되는 Agent ZIP 패키지 형식으로 수정했습니다.
- Fixed the Agent ZIP package format so it extracts correctly on both Linux and Windows.

- 릴리스 SHA-256 파일을 표준 `SHA256  filename` 형식으로 발행합니다.
- Release SHA-256 files now use the standard `SHA256  filename` format.

## 검증 / Verification

- Agent ZIP 내부 경로, SHA-256, Windows 자동 업데이트 적용을 검증했습니다.
- Verified Agent ZIP entry paths, SHA-256, and a Windows automatic-update installation.
